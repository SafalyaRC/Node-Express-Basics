const path=require('path')

console.log(path.sep);

// path.resolve(_dirname,'folder','filename') --> returns the absolute path of the file specified 
// path.join('folder','file') --> joins the paths